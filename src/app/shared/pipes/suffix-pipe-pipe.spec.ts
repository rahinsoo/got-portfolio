import { SuffixPipePipe } from './suffix-pipe-pipe';

describe('SuffixPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SuffixPipePipe();
    expect(pipe).toBeTruthy();
  });
});
